// Screens de Navegacion.
const home = {
    tab: "HomeTab",
    home: "Home",
};

const trends = {
    tab: "TrendsTab",
    home: "Trend",
};

const notifications = {
    tab: "NotificationsTab",
    home: "notifications",
};

const account = {
    tab: "AccountTab",
    home: "account",
};


//Screens Auth.
const auth = {
    auth: "Auth",
}


//return
export const screen = {
    home,
    trends,
    notifications,
    account,
    auth,
};